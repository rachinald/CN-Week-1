


const dataChecker = (string, rank) => {
    if ((string == "codenation") && (rank == 1)){
        console.log(`This is the string: ${string} and the rank is: ${rank}`);
    } else {
        console.log(`Does not match`);
    }
}
dataChecker ("codenation", 1);

